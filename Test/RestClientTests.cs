using System;
using Xunit;
using Code;
using System.Net.Http;

namespace Test
{
    public class UnitTest1
    {
        string url = "https://www.google.com";
        //[Fact]
        /*public void WhenInputURLItShouldRunGetMethodAndReturnStatus()
        {
               RestClient restClient = new RestClient();
               var statusCode = restClient.Execute(url);
               Assert.Equal(HttpsStatusCode.Ok, statusCode);

        }*/
    }
}
